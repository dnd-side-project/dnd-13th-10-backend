package com.seed.domain.memoir.repository;

import com.seed.domain.memoir.entity.MemoirViewHist;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MemoirViewHistRepository extends JpaRepository<MemoirViewHist, Long> {
}
